# Точечная подгонка кегля для русских строк, не влезающих в свой прямоугольник.
# Формат: подстрокаИмениОбъекта|scale|enableWrap|maxLines
#   scale 0..1 — доля исходного кегля (напр. 0.8 = 80%); 0 или >=1 — не масштабировать.
#   enableWrap true — включить перенос слов; maxLines >0 — ограничить число строк.
# По умолчанию (без правила) UI не трогается — русский влезает в большинство
# родных прямоугольников. Правила добавляются точечно по проблемным экранам.
#
# Пример: ужать конкретную надпись до 85% и разрешить перенос:
#   SomeLabel|0.85|true|2

# Лор-описание вида (char-select, GameObject "Desc1"): русский длиннее EN,
# хвост-предупреждение («Do not treat lightly» и т.п.) не влезал у Дюна/Чик/Ике.
Desc1|0.95|true|0
